const {HOST, PORT, PROTOCOL} = {
    HOST: "localhost",
    PORT: "8000",
    PROTOCOL: "http"
}

const {INDEX, CATEGORY, CATALOG, PROFILE} = {
    INDEX: "index",
    CATEGORY: "category",
    CATALOG: "catalog",
    PROFILE: "profile",
}
