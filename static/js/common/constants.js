const {HOST, PORT, PROTOCOL} = {
    HOST: "127.0.0.1",
    PORT: "8000",
    PROTOCOL: "http"
}

const {INDEX, CATEGORY, CATALOG, PROFILE} = {
    INDEX: "index",
    CATEGORY: "category",
    CATALOG: "catalog",
    PROFILE: "profile",
}
